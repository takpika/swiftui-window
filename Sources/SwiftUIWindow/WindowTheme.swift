import SwiftUI

enum WindowTheme {
    static let background = Color(uiColor: .secondarySystemBackground)
    static let activeBar = Color(uiColor: .systemGray4)
    static let inactiveBar = Color(uiColor: .systemGray5)
    static let previewFill = Color(uiColor: .systemBackground)
}
